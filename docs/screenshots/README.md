# Screenshot checklist

Referenced from the root `README.md`. Capture at **1440px wide**, light theme, sample data only (no real PII).

| File | What to capture |
|------|-----------------|
| `customer-transfer.png` | Customer portal → internal transfer form with fee/limit quote visible |
| `customer-history.png` | Customer portal → transfer history list |
| `admin-monitor.png` | Admin portal → transaction monitor list with status filters |
| `admin-report.png` | Admin portal → transaction report (daily volume / status breakdown) |
| `grafana.png` | Grafana dashboard with service metrics (Prometheus datasource) |

Optional extras: `admin-recon.png` (reconciliation runs + discrepancy list), `admin-rbac.png` (role/permission matrix), `zipkin-trace.png` (one transfer trace across services).
