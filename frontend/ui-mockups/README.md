# UI mockups (design prototype only)

**This folder is not the product frontend.**

- Static HTML/CSS/JS gallery used for IA / UX review before Angular screens were built.
- All copy and numbers here are **hard-coded sample content** on purpose (fixtures for design).
- They are **not** i18n keys and **not** loaded from the database.
- The real app is `frontend/bank-angular-app/` (API + `public/i18n/{vi,en}.json`).

Open `index.html` in a browser only when reviewing layout/spec. Do not treat it as runtime banking UI.
